#!/bin/bash

# Let's Encrypt traditional directories
CERT=/etc/letsencrypt/live/topicsexplorer.brazilsouth.cloudapp.azure.com/fullchain.pem;
KEY=/etc/letsencrypt/live/topicsexplorer.brazilsouth.cloudapp.azure.com/privkey.pem;

sudo openssl pkcs12 -export -in ${CERT} -inkey ${KEY} -name topicsexplorercert -out topicsexplorercert.p12

sudo chown ${VM_USERNAME}:${VM_USERNAME} topicsexplorer.p12

# Create a keystore


# keytool -importkeystore -srckeystore topicsexplorercert.p12 -destkeystore ${SPRINGKEYSTORE} -srcstoretype pkcs12 -alias topicsexplorer

rm topicsexplorercert.p12